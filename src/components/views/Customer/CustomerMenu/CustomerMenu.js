import React from 'react';

const CustomerMenu = props => {
    return (
        // ? To form or not To form
        <form
        onSubmit = {
         e => {e.preventDefault()}
        }
        >

        </form>
    );
}


export default CustomerMenu;
